/// Explicit runtime lock/artifact API.
///
/// The implementation remains in [runtime_delivery.dart] during the PR-2
/// compatibility window so the legacy delivery tests and the new verifier
/// share the same archive/security primitives.
library;

export 'runtime_delivery.dart';
