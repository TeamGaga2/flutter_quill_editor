# Rich Text Authoring

TeamGaga 富文本创作上下文定义编辑内容中的排版语义，使同一种内容在不同宿主和屏幕形态下保持一致含义。

## Language

**正文基础排版（T）**:
富文本内容中所有正文类文字共享的基础排版语义；移动端与桌面端可以采用各自的设计规格，段落、列表和引用在此基础上增加结构样式。
_Avoid_: 普通段落、默认字号

**顶层内容块**:
编辑内容流中参与块间距计算的结构单元；段落、标题、整个列表、整个引用组、分隔线和媒体各自构成一个顶层内容块，空段落仍视为段落。
_Avoid_: 行、列表项、引用行

**块间距**:
顶层内容块之间的垂直间隔，由后一个内容块的顶部间距表达；列表项之间和同一引用组的连续引用行之间不属于块间距。
_Avoid_: 行距、段落上下间距

**引用组（Quote）**:
由一行或多行连续引用内容组成的顶层内容块；连续引用行共同形成一个视觉整体，而不是多个独立内容块。
_Avoid_: 引用行、Blockquote 节点

**编辑表面**:
承载富文本标题、正文和编辑器内工具栏的连续视觉背景；宿主容器与嵌入文档属于同一个编辑表面，在相同主题下必须使用同一语义颜色。
_Avoid_: WebView 背景、Flutter 背景

**链接浮层（Link Popover）**:
富文本运行时内渲染的链接表单浮层，用于新增或修改链接；桌面形态锚定在选区附近，移动形态以居中模态呈现，背景与边框使用与编辑表面一致的语义颜色变量。
_Avoid_: 链接对话框、宿主链接弹窗、Link Dialog

**富文本运行时发行版**:
由富文本 WebView 运行时的某次成功分支构建生成、带有来源 commit 和内容摘要的一组完整预制资源。
_Avoid_: 构建目录、dist

**运行时发布通道**:
宿主应用在构建时跟随的 richtext 分支；每次构建解析该分支最新成功的富文本运行时发行版。
_Avoid_: 运行时版本锁、固定资产副本
